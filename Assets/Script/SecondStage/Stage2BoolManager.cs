using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Stage2BoolManager : MonoBehaviour
{
    public bool is_Combine;
    // Start is called before the first frame update
    void Start()
    {
        is_Combine = false;
    }

    // Update is called once per frame
    void Update()
    {

    }
    
}
